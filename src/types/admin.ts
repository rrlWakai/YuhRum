export type Room = {
  id: string;
  name: string;
  category: string;
  description: string;
  features: string;
  price: string;
  ctaLabel: string;
  image: string;
};

export type Amenity = {
  id: string;
  title: string;
  text: string;
};
